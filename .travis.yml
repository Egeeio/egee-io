language: ruby
rvm:
  - 2.5
before_install:
  - gem install bundler
  - gem install rake
install:
  - bundle install
  - rake build
deploy:
  - provider: script
    skip_cleanup: true
    # script: bash terraform.sh apply -auto-approve
    # on:
    #   tags: true