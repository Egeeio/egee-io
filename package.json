{
  "name": "egeeio-website",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "./node_modules/.bin/http-server ./public",
    "build": "./node_modules/.bin/pug ./src --out ./public && ./node_modules/.bin/sass --no-source-map ./src/site.sass:public/site.css --style compressed",
    "start": "npm run build && npm run serve"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Egeeio/egeeio-website.git"
  },
  "author": "egee",
  "license": "MIT",
  "homepage": "https://egee.io",
  "devDependencies": {
    "sass": "^1.26.10",
    "http-server": "^0.12.3",
    "pug-cli": "^1.0.0-alpha6"
  }
}
