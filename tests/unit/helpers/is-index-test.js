import { isIndex } from 'egeeio/helpers/is-index';
import { module, test } from 'qunit';

module('Unit | Helper | is index');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isIndex([42]);
  assert.ok(result);
});
