import { personaName } from 'egeeio/helpers/persona-name';
import { module, test } from 'qunit';

module('Unit | Helper | persona name');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = personaName([42]);
  assert.ok(result);
});
