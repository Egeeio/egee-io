import { gameSquare } from 'egeeio/helpers/game-square';
import { module, test } from 'qunit';

module('Unit | Helper | game square');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = gameSquare([42]);
  assert.ok(result);
});
