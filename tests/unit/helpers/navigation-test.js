import { navigation } from 'egeeio/helpers/navigation';
import { module, test } from 'qunit';

module('Unit | Helper | navigation');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = navigation([42]);
  assert.ok(result);
});
