import { gameTitle } from 'egeeio/helpers/game-title';
import { module, test } from 'qunit';

module('Unit | Helper | game title');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = gameTitle([42]);
  assert.ok(result);
});
