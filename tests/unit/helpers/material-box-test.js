import { materialBox } from 'egeeio/helpers/material-box';
import { module, test } from 'qunit';

module('Unit | Helper | material box');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = materialBox([42]);
  assert.ok(result);
});
