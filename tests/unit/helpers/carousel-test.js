import { carousel } from 'egeeio/helpers/carousel';
import { module, test } from 'qunit';

module('Unit | Helper | carousel');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = carousel([42]);
  assert.ok(result);
});
