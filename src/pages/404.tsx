import * as React from "react";
import Layout from "../components/layout";

const NotFoundPage = () => (
  <Layout>
    <h2>NOT FOUND</h2>
    <p>You just hit a route that doesn&#39;t exist... Yes, I know the styling is broken. I'll fix it later.</p>
  </Layout>
)

export default NotFoundPage
